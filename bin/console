#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "shout"

puts "initializing..."

client = Shout::ShoutClient.new("source", "hackme!", "localhost", 8000, "/stream")
client.begin do
  play("bin/oOi3oJmfz4o.mp3")
end
